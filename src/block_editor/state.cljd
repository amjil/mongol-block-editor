(ns block-editor.state
  (:require 
   ["package:flutter/material.dart" :as m]))

;; block ids vector
(def ids (atom []))

(def blocks (atom {}))

(def current-id (atom nil))

(def search-result (atom []))
                          
(def search-query (atom ""))

(def open-search-box (atom false))
                          

(def layer-link (m/LayerLink))